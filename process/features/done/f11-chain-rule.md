# Feature description for feature F11
## F11 — lesson: Chain Rule & Partial Derivatives
**Priority**: High
**Done:** yes
**Tests Written:** yes
**Test Passing:** yes
**Description**: Add a lesson that introduces the chain rule and partial derivatives interactively. Use the (x²)³ example from Ch. 4: show the two-step computation graph, run the forward pass to get values at each node, then show how a tiny change at x propagates forward. Prose explains: local derivative at each node, chain rule as multiplying local derivatives back to front, and what a partial derivative means (hold everything else fixed). No turkey data in this lesson — keep it simple and abstract.
