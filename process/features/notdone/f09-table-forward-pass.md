# Feature description for feature F09
## F09 — Table-based forward pass lesson
**Priority**: High
**Done:** no
**Tests Written:** no
**Test Passing:** no
**Description**: Replace the per-turkey graph step-through with a compact HTML table showing all three turkeys as rows and all computation nodes as columns. Clicking Next advances one step across all rows simultaneously. Uncomputed cells show "—". The active column is highlighted gold. A single explanation line below the table describes the current step. The static graph from The Model lesson is kept as a structural reference above. All previous graph/mermaid machinery is preserved unchanged.
