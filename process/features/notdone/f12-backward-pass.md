# Feature description for feature F12
## F12 — lesson: The Backward Pass
**Priority**: High
**Done:** no
**Tests Written:** no
**Test Passing:** no
**Description**: Add a lesson after F11. Show the completed 3-turkey forward pass table (step=4). Add a "Run Backward Pass" button. When clicked, run backward_pass on a turkey_feather graph and redisplay the Mermaid graph with gradient values annotated on each node. Prose connects the gradient on w1 (1875) and w2 (3500) back to the weight update rule: subtract learning_rate × gradient. Reader sees the full training loop close: forward → loss → backward → update.
